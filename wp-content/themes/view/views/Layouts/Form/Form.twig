{#{{ dump() }}#}
{% set classes = [
    layout,
] %}
<section class="{{ classes|join(' ') }}">
    <div class="container">
        <div class="{{ layout }}__form">
            {{ gform(form) }}
        </div>
    </div>
</section>