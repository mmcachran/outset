{# {{dump()}} #}
{% if items %}
    <ul class="menu flex flex-wrap">
        {% for item in items %}
            {% include "component/menu-item/menu-item.twig" with item only %}
            <ul class="">
                {% for child in item.children %}
                    {% include "component/menu-item/menu-item.twig" with child only %}
                {% endfor %}
            </ul>
        {% endfor %}
    </ul>
{% endif %}
